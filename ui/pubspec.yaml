name: bom_bar_ui
description: User interface for BOM-bar service

publish_to: 'none'

version: 0.1.0+4

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  pedantic: ^1.11.1
  provider: ^5.0.0
  dio: ^5.0.3
  http: ^0.13.3
  badges: ^2.0.1
  intl: ^0.17.0
  pie_chart: ^5.0.0
  url_launcher: ^6.0.3
  collection: ^1.15.0
  yeet: ^0.4.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.0.10
  build_runner: ^2.0.1

flutter:
  uses-material-design: true

